#ifndef SEQUENTIAL_FILTER_H
#define SEQUENTIAL_FILTER_H

/* Function that runs the sequential filtering.
 * Returns 0 on success, nonzero otherwise.
 */
int run_sequential_filter(char *input_filename, char *output_filename);

#endif
